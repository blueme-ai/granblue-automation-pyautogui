# PyTorch - CPU 版本（無需顯卡也能跑）；有 NVIDIA GPU 可自行改裝 CUDA 版
# Intel Mac 只支援到 2.2.x（PyTorch 2.3 起停止提供 x86_64 macOS 版本）
torch>=2.1.0; sys_platform != "darwin" or platform_machine == "arm64"
torchvision>=0.16.0; sys_platform != "darwin" or platform_machine == "arm64"
torchaudio>=2.1.0; sys_platform != "darwin" or platform_machine == "arm64"
torch>=2.1.0,<2.3; sys_platform == "darwin" and platform_machine == "x86_64"
torchvision>=0.16.0,<0.18; sys_platform == "darwin" and platform_machine == "x86_64"
torchaudio>=2.1.0,<2.3; sys_platform == "darwin" and platform_machine == "x86_64"

PyAutoGUI>=0.9.54
Pillow>=10.0.0
opencv-python>=4.8.0
easyocr>=1.7.1
numpy>=1.26.0
playsound3>=1.0.0
pyclick>=0.0.2
pyperclip>=1.8.2
tweepy>=4.14.0
discord.py>=2.3.2
dictor>=0.1.11
async_lru>=2.0.4
websockets>=12.0

# GUI（跨平台，Windows / Intel Mac / Apple Silicon 皆支援）
PySide6>=6.6
# 全域停止快捷鍵（mac 需在系統設定授權「輔助使用」）
keyboard>=0.13

# 僅 Windows 需要
pywin32>=306; sys_platform == "win32"
