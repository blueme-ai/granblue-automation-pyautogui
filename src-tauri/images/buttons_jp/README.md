# 日文版按鈕模板

GUI「遊戲語言」選「日本語」時，比對按鈕會優先使用這個資料夾裡的同名模板，
找不到就自動退回英文版 `../buttons/`。

只有**含文字**的按鈕需要日文版模板（例如 quest、special、ok、play、attack 等）；
純圖示按鈕（home、召喚石元素圖、地圖節點等）跨語言通用，不用重做。

## 製作方式

1. 遊戲視窗維持 100% 縮放（瀏覽器縮放 100%），截整個遊戲畫面 PNG
2. 從截圖裁下按鈕，存成與英文版同名的 `.jpg`（檔名見 `../buttons/`）
3. 邊緣留一點底色、不要裁太貼字
